---
description: 先写流程、原理与方案，确认后再改代码（LinkAgent 强制开发流程）
alwaysApply: true
---

# 先方案后代码

在本仓库中，**默认禁止**在未向用户说明下列内容前就编辑/新增源码或测试：

1. **原理 / 现状**：相关行为、根因或约束；引用本仓库真实路径与调用关系，避免空泛猜测。
2. **流程**：端到端怎么做（用户/进程/API/持久化顺序）；多角色或多进程须写清边界。
3. **方案**：改什么、不改什么、影响范围；是否符合 `AGENTS.md` 分层与鉴权约定。

用户已明确「按上面方案实现 / 直接改」时，视为方案已确认，可进入实现阶段。

**可跳过完整三节的情况**：只读解释、review、用户未要求改库；或极 trivial 修复且已在回复中用一句话说清原因。

实现阶段仍须：最小改动、新增功能补测试、严格 TS 与 ESM 约定。细则见仓库根目录 `AGENTS.md` 的「开发流程（强制）」。
